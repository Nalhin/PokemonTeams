const initialPadding = 8;

export const PADDING = {
  EXTRA_SMALL: `${initialPadding / 4}px`,
  SMALL: `${initialPadding / 2}px`,
  BASE: `${initialPadding}px`,
  LARGE: `${initialPadding * 2}px`,
};
